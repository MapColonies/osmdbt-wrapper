export enum FsSpanName {
  FS_MKDIR = 'fs.mkdir',
  FS_WRITE = 'fs.write',
  FS_APPEND = 'fs.append',
  FS_READ = 'fs.read',
  FS_RENAME = 'fs.rename',
  FS_UNLINK = 'fs.unlink',
}

export enum FsAttributes {
  FILE_PATH = 'file.path',
  FILE_NAME = 'file.name',
  DIR_PATH = 'dir.path',
  DIR_MK_COUNT = 'dir.mk.count',
}
